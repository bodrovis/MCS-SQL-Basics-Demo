<!DOCTYPE html>
<html class="h-100" lang="ru">
<head>
  <meta charset="utf-8">
  <title>SocialNetwork</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <link href="https://fonts.googleapis.com/css?family=IBM+Plex+Mono|Press+Start+2P&display=swap&subset=cyrillic" rel="stylesheet">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body class="d-flex flex-column h-100">

<main class="flex-shrink-0">
  <nav class="navbar navbar-light bg-light navbar-expand-lg mb-5">
    <%= link_to 'Друзья по коду', root_path, class: 'navbar-brand mr-auto' %>

    <ul class="navbar-nav">
      <li class="nav-item">
        <%= link_to 'Создать пользователя', new_user_path, class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <%= link_to 'Все пользователи', users_path, class: 'nav-link' %>
      </li>
    </ul>
  </nav>

  <div class="container">
    <% flash.each do |k, v| %>
      <%= tag.div v, class: "alert alert-#{k}" %>
    <% end %>

    <%= yield %>
  </div>
</main>

<footer class="mt-auto py-3">
  <div class="container-fluid">
    <div class="row">
      <div class="col">
        Основы SQL для Moscow Coding School
      </div>

      <div class="col text-right">
        &copy; 2019
      </div>
    </div>
  </div>
</footer>

</body>
</html>
